# ============================================================
# CONSTRU-TRANS_01 — VARIABLES DE ENTORNO LISTAS PARA USAR
# ------------------------------------------------------------
# Copia este archivo como `.env` dentro de la carpeta raíz del
# proyecto Constru-trans_01 (al lado de manage.py) y Django
# usará automáticamente la BD Neon y enviará correos de
# recuperación de contraseña con la cuenta construtrans588.
# ============================================================

# --- DJANGO ---
# Genera una nueva con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=cambia-esto-por-una-clave-aleatoria-de-50-caracteres
DEBUG=True
DJANGO_ENV=development
DJANGO_SETTINGS_MODULE=core.settings

ALLOWED_HOSTS=127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000

# ============================================================
# BASE DE DATOS NEON (PostgreSQL en la nube)
# ============================================================
# URL completa que Django (dj_database_url) usa por defecto.
DATABASE_URL=postgresql://neondb_owner:npg_GTgrS3XtIk5J@ep-fragrant-dew-anpj34eu-pooler.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Variables sueltas (por si algún script las pide individualmente)
DB_ENGINE=django.db.backends.postgresql
DB_HOST=ep-fragrant-dew-anpj34eu-pooler.c-6.us-east-1.aws.neon.tech
DB_PORT=5432
DB_NAME=neondb
DB_USER=neondb_owner
DB_PASSWORD=npg_GTgrS3XtIk5J
DB_SSLMODE=require

# ============================================================
# EMAIL — RECUPERACIÓN DE CONTRASEÑA (Gmail)
# ============================================================
# ¡IMPORTANTE! EMAIL_BACKEND debe ser smtp para que los correos
# realmente se envíen. Si lo dejas en blanco, Django solo los
# imprime en consola y los usuarios nunca reciben el enlace.
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=construtrans588@gmail.com
EMAIL_HOST_PASSWORD=gtdquhxowjslishw
DEFAULT_FROM_EMAIL=Constru-Trans <construtrans588@gmail.com>
SERVER_EMAIL=Constru-Trans <construtrans588@gmail.com>
EMAIL_TIMEOUT=30

# --- SEGURIDAD ---
PASSWORD_RESET_TIMEOUT=1800
SESSION_COOKIE_AGE=1209600
AXES_FAILURE_LIMIT=5
AXES_COOLOFF_TIME=1

# --- LICENCIAMIENTO (opcional) ---
LICENSE_REQUIRED=False
LICENSE_SERVER_URL=
LICENSE_PUBLIC_KEY=
LICENSE_HEARTBEAT_MINUTES=360
LICENSE_MAX_AGE_DAYS=30